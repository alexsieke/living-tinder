<nav class="navbar navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if params[:controller] == "tinders" %>
        <%= link_to 'Sign in as new user', root_path, class: 'navbar-brand' %>
      <% end %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <% if !session[:current_user_id].nil? && params[:controller] == "tinders" %>
          <li><a>Yo, <%= session[:current_user_name] %>, Time to Start Tindering</a></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
